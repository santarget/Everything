<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssy.everything">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />

    <meta-data
        android:name="com.ssy.everything.image.skip_cer_verify.UnsafeOkHttpGlideModule"
        android:value="GlideModule" />
    <application
        android:name=".EverythingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".feature.home.view.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppWindowTranslucentStatusTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.home.view.activity.UserActivity"
            android:theme="@style/AppTheme.Transparent" />
        <activity
            android:name=".feature.news.view.activity.NewsActivity"
            android:theme="@style/AppTheme.Transparent" />
        <activity
            android:name=".feature.news.view.activity.NewsDetailActivity"
            android:theme="@style/AppTheme.Transparent" />

    </application>

</manifest>